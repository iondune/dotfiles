
# If not running interactively, don't do anything
case $- in
    *i*) ;;
      *) return;;
esac

# save default path
export DEFAULT_PATH=$PATH

# include bashrc
[[ -r "$HOME/.bashconfig" ]] && . "$HOME/.bashconfig"
